name: try
on:
  push
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: Mask
        run: |
          X=$(jq -r ".before" $GITHUB_EVENT_PATH)
          echo "add-mask::$X"
      - name: Use
        run: |
          echo ${{ github.event.before }}
          echo ${{ github.event.after }}
